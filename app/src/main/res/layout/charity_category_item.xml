<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:orientation="horizontal">
    <!--<TextView
        android:id="@+id/blank"
        android:layout_width="10dp"
        android:layout_height="wrap_content"
        android:text="这个是为了美观隔断一下 只在最左侧放一个 删了"
        android:textColor="@color/white"
        android:textSize="12dp"
        android:textStyle="bold" />-->

    <RelativeLayout
        android:id="@+id/charity_category_it"
        android:layout_width="160dp"
        android:layout_height="120dp">

        <ImageView
            android:id="@+id/id_index_gallery_item_image"
            android:layout_width="150dp"
            android:layout_height="100dp"
            android:layout_centerVertical="true"
            android:layout_centerInParent="true"
            android:scaleType="centerCrop"
            android:src="@drawable/thumbnail21" />

        <TextView
            android:id="@+id/id_index_gallery_item_text"

            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/id_index_gallery_item_image"
            android:layout_alignEnd="@+id/id_index_gallery_item_image"
            android:background="@color/alpha_blue"
            android:gravity="center"
            android:text="青少年服务"
            android:textColor="@color/white"
            android:textSize="12dp"
            android:textStyle="bold" />
    </RelativeLayout>
</LinearLayout>

